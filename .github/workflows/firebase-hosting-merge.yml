# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/front
    steps:
      - name: Checkout
        uses : actions/checkout@v2

      - name: Create dot env file
        run: |
          touch .env
          echo "FB_API_KEY=${{ secrets.FIREBASE_API_KEY }}" >> .env
          echo "FB_AUTH_DOMAIN=${{ secrets.FIREBASE_AUTH_DOMAIN }}" >> .env
          echo "FB_PROJECT_ID=${{ secrets.FIREBASE_PROJECT_ID }}" >> .env
          echo "FB_STORAGE_BUCKET=${{ secrets.FIREBASE_STORAGE_BUCKET }}" >> .env
          echo "FB_MESSAGING_SENDER_ID=${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}" >> .env
          echo "FB_APP_ID=${{ secrets.FIREBASE_APP_ID }}" >> .env
          echo "FB_MEASUREMENT_ID=${{ secrets.FIREBASE_MEASUREMENT_ID }}" >> .env

      - name: Dependency install
        run: |
          npm ci

      - name: deploy
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TECH_TWEET_374313 }}'
          channelId: live
          projectId: tech-tweet-374313
          entryPoint: src/front
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks